When starting new React project, run `npm install` and `npm update` in VSCode terminal.

To start server: `npm start`. To open site click on "Local host" while holding down Ctrl.

To generate static files to publish for live website. `npm run build`

To stop any terminal services, press Ctrl C. example (To stop server running in Terminal)